eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8084/eureka/
    registerWithEureka: true
    fetchRegistry: true
  instance:
    hostname: localhost
    prefer-ip-address: true

spring:
  application:
    name: dev-config
